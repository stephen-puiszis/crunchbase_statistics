<%= form_for(@investment) do |f| %>
  <% if @investment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@investment.errors.count, "error") %> prohibited this investment from being saved:</h2>

      <ul>
      <% @investment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :funding_id %><br />
    <%= f.number_field :funding_id %>
  </div>
  <div class="field">
    <%= f.label :investable_id %><br />
    <%= f.number_field :investable_id %>
  </div>
  <div class="field">
    <%= f.label :investable_type %><br />
    <%= f.text_field :investable_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
